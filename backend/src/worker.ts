/**
 * Legacy BullMQ workers disabled — business logic lives on-chain.
 * Run `npm run dev` (server.ts) for the blockchain indexer + analytics API.
 */
console.log("[worker] Legacy MLM workers disabled. Use server indexer-only mode.");
